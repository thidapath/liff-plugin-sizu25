// Import stylesheets
import './style.css';

class BMIPlugin {
  constructor() {
  }
  
  install(context, option) {
  }

  localDate(lang, date) {
  }

  calculate(height, weight) {
  }

  greeting(lang, profile) {
  }
}

// Binding HTML elements
const h1Element = document.querySelector("h1");
const h2Element = document.querySelector("h2");
const h5Element = document.querySelector("h5");
const heightElement = document.querySelector("#SYS");
const weightElement = document.querySelector("#DIA");
const btnElement = document.querySelector("button");

// Activate LIFF Plugin

// Display locale date

// Add event listerner to Calculate button